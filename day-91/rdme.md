# how we can store a data in database
- what data and which format you store a data in database thats depend on u
- this process means creating a schema
we tell to database to save notes data
notes = {
    title = string 
    description= string
    }
- we are telling to database to which format save our data is called create schema    

- in database if we want to perform cruid operations then we need a models
CRUD OPERATION
- DATA CREATIN,READING,UPDATING,DELETING
- FOR RUNNIG THIS WE NEED TO CREATE MODEL
- MULTIPLE NODES IN DATABASE IN EACH DATA FORMAT HAVE TITLE AND DESCRIPTION
- whenewcer we ceating a note database gives a id to the notes for identify notes it gives unique id
- this id is unique and never the two node id is same