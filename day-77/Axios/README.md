# CSR (client side rendering)

# SSR (server side rendering)


// Examples

// ✅ Q1. Difference of two numbers

// 👉 Write a function that accepts two integers and prints their difference.

function difference(a, b) {
    console.log(a-b);
  }
  
  difference(20, 10);
  
  // output: 10