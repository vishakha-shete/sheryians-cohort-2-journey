FROM node:18-alpine
WORKDIR /app

# Cached layer — only re-runs when package.json changes
COPY package*.json ./
RUN npm install

# Source code layer
COPY . .

EXPOSE 3000

CMD ["npx", "nodemon", "server.js"]

